package com.acme.billcalc.service;

import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.junit.jupiter.MockitoExtension;

@ExtendWith(MockitoExtension.class)
class BillToCoinCalculatorServiceTest {
    /*
    private final CoinsConfiguration coinsConfiguration = new CoinsConfiguration();
    private final BillsConfiguration billsConfiguration = new BillsConfiguration();

    private final BillToCoinCalculatorService billToCoinCalculatorService = new BillToCoinCalculatorService(coinsConfiguration, billsConfiguration);


    @Test()
    void billToCoinCalculator() {
        Map<Integer, Integer> bills = Map.of(1, 7,2,4,5,1);
        billToCoinCalculatorService.billToCoinCalculator(bills, false);
    }

    @Test
    void validateAmount() {
        Map<Integer, Integer> bills = Map.of(1, 7,2,4,7,1);
        billToCoinCalculatorService.validateBills(bills);
    }*/
}